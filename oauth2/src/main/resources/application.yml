spring:
  application.name: oauth2

  profiles:
    active: dev

  config:
    import: "optional:configserver:"
  cloud:
    config:
      uri: http://localhost:8888

logging:
  level:
    org:
      springframework:
        security: DEBUG